# Local business template library

React/Vite source for the public gallery at `/templates/`.

The initial template families and preview captures were copied from the local-business templates workspace. This repository owns the public catalog presentation and its deployable build.

## Build

```bash
npm ci
npm run build
```

Vite writes the static production output to the repository-level `templates/` directory so GitHub Pages can serve it directly.
